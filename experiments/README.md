# experiments/

One subdirectory or script per paper experiment. Each should be reproducible
from a pinned Hydra config + seed list and produce artifacts under `results/`
(gitignored). Keep figure-generation scripts here so paper plots are
regenerable from logged metrics.
